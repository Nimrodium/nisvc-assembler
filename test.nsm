:entry
    _start

:data
    hello str "Hello, World!" $10 ;
    len equ (. - $!hello);

:program
    !_start
        ldi r1, @!hello
        ldi r2, $!len
        push r2
        push r1
        int $2
        int $255
